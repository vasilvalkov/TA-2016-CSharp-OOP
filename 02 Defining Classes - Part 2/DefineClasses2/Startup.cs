namespace DefineClasses2
{
    using Matrix;
    // Class members ordering in this project is strictly based on StyleCop v4.5 Rules Documentation(http://stylecop.soyuz5.com/Ordering%20Rules.html). The rules are nicely explained in this thread (http://stackoverflow.com/questions/150479/order-of-items-in-classes-fields-properties-constructors-methods).
    //
    class Startup
    {
        //
        // Summary:
        //     In this homework problems of same context are grouped in separate class libraries. 
        //     There are four class libraries for the four contexts of tasks.
        //     Tests are written for each of the problems to make evaluation easier.
        //
        // Returns:
        //     All tests will write their result on the console.
        //
        // Execution:
        //     To test the tasks, please uncomment the run methods below for each of the problem groups.
        static void Main()
        {
            // Problems 1-4
            //Models3DTest.Run();

            // Problems 5-7
            //GenericClassTest.Run();

            // Problems 8-10
            //MatrixTest.Run();

            // Problem 11
            //VersionAttribute.ClassVersionQuery(typeof(MatrixTest));
            //VersionAttribute.MethodsVersionQuery(typeof(MatrixTest));
            //VersionAttribute.ClassVersionQuery(typeof(Models3DTest));
            //VersionAttribute.ClassVersionQuery(typeof(Matrix<>));
        }
    }
}